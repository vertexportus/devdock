if [ -f ".env" ]; then dotenv; else echo "no .env file"; fi
PATH_add "$PWD/bin"

# project/docker
export PROJECT_PATH=$PWD
export DEVDOCK_PATH=$PROJECT_PATH/devdock
source $DEVDOCK_PATH/env/generate_envs.sh
